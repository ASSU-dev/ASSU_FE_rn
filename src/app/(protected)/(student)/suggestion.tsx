import { Text, View } from "react-native";

export default function StudentSuggestionScreen() {
	return (
		<View className="flex-1 items-center justify-center bg-canvas">
			<Text className="text-content-primary font-medium">제휴건의함</Text>
		</View>
	);
}
