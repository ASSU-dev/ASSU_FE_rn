// 제휴 건의 폼 라우트 — suggestion 탭에서 진입하는 Stack 스크린이다
import { StudentSuggestionFormPage } from "@/pages/student/suggestion-form";

export default function StudentSuggestionFormScreen() {
	return <StudentSuggestionFormPage />;
}
